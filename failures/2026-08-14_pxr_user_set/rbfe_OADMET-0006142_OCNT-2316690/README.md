This transformation has very low spatial overlap, resulting in a single mapped heavy atom, 
while the simulation does run in the solvent leg it has a large MBAR error (~2.3 kcal/mol) and 
pymbar prints many warnings about struggling to converge this also makes each iteration slower.
